# 模块 08：质量验证

[← 返回主文档](../AI_AGENT_视觉还原_SOP_3.1_主文档.md)

---

## 🎯 目标

全面验证React项目的视觉还原度和内容完整性。

---

## 📋 验证清单

### 1. 构建验证

**测试项目是否可以正常构建和运行**

```bash
# 步骤1：安装依赖
npm install

# 预期结果：
✓ 无错误
✓ 所有依赖安装成功
```

```bash
# 步骤2：TypeScript类型检查
npm run build

# 预期结果：
✓ 0 TypeScript错误
✓ 构建成功
✓ dist文件夹生成
```

```bash
# 步骤3：启动开发服务器
npm run dev

# 预期结果：
✓ 服务器成功启动
✓ 端口正常（通常5174）
✓ Console无error
```

**记录结果**：

```json
{
  "build_test": {
    "npm_install": "success",
    "typescript_errors": 0,
    "build_success": true,
    "dev_server": "running",
    "port": 5174
  }
}
```

---

### 2. 内容完整性验证 ⭐⭐⭐

**最关键的验证步骤**

#### 步骤1：打开浏览器

```
访问: http://localhost:5174
并排打开原网站进行对比
```

#### 步骤2：逐Section检查

```
□ Navigation
  □ Logo显示正确
  □ 所有菜单项都存在
  □ CTA按钮存在

□ Section 1 (Hero)
  □ 标题文字正确
  □ 描述文字正确
  □ 图片显示正确
  □ 按钮文字和链接正确
  □ ❌ 不是 "Content goes here"

□ Section 2
  □ 标题存在
  □ 所有卡片都渲染了（数量匹配原网站）
  □ 每个卡片内容完整
  □ ❌ 不是占位符

□ Section 3
  □ 内容完整
  □ 图片显示
  □ 文字正确
  
... 继续检查所有section

□ Footer
  □ 所有链接组都存在
  □ 版权信息正确
```

#### 步骤3：使用检查脚本

```python
def verify_content_completeness(url='http://localhost:5174'):
    """验证内容完整性"""
    from selenium import webdriver
    from bs4 import BeautifulSoup
    
    # 启动浏览器
    driver = webdriver.Chrome()
    driver.get(url)
    
    # 等待页面加载
    import time
    time.sleep(2)
    
    # 获取页面HTML
    html = driver.page_source
    soup = BeautifulSoup(html, 'html.parser')
    
    results = {
        'sections_count': 0,
        'placeholders_found': [],
        'empty_sections': [],
        'missing_images': []
    }
    
    # 检查section数量
    sections = soup.find_all('section')
    results['sections_count'] = len(sections)
    
    # 检查占位符
    placeholder_keywords = [
        'content goes here',
        'todo',
        'placeholder',
        'lorem ipsum',
        'sample text'
    ]
    
    for i, section in enumerate(sections, 1):
        text = section.get_text().lower()
        
        # 检查占位符
        for keyword in placeholder_keywords:
            if keyword in text:
                results['placeholders_found'].append({
                    'section': i,
                    'keyword': keyword
                })
        
        # 检查是否为空section
        if len(text.strip()) < 50:  # 少于50个字符可能是空的
            results['empty_sections'].append(i)
        
        # 检查图片
        images = section.find_all('img')
        for img in images:
            src = img.get('src', '')
            if not src or src.startswith('data:') or 'placeholder' in src:
                results['missing_images'].append({
                    'section': i,
                    'src': src
                })
    
    driver.quit()
    
    return results

# 执行验证
results = verify_content_completeness()

# 判断是否通过
passed = (
    results['sections_count'] >= 13 and  # 至少13个section
    len(results['placeholders_found']) == 0 and  # 无占位符
    len(results['empty_sections']) == 0 and  # 无空section
    len(results['missing_images']) == 0  # 无缺失图片
)

print(f"内容完整性验证: {'✓ 通过' if passed else '✗ 失败'}")
print(f"Section数量: {results['sections_count']}")
print(f"发现占位符: {len(results['placeholders_found'])}")
print(f"空section: {len(results['empty_sections'])}")
print(f"缺失图片: {len(results['missing_images'])}")
```

---

### 3. 视觉对比验证

**对比原网站和React项目的视觉效果**

#### 颜色验证

```python
def verify_colors():
    """验证颜色是否匹配"""
    # 对比design_tokens.json中的颜色和实际渲染的颜色
    
    checklist = {
        'primary_color': '检查主色是否一致',
        'secondary_color': '检查辅色是否一致',
        'text_color': '检查文字颜色是否一致',
        'background_colors': '检查背景色是否一致'
    }
    
    # 可以使用截图对比工具
    # 或手动视觉检查
    
    return checklist
```

**手动检查清单**：

```
□ 主色调匹配
□ 辅色调匹配
□ 文字颜色匹配
□ 背景色匹配
□ 链接颜色匹配
□ 按钮颜色匹配
```

#### 字体验证

```
□ 字体家族正确
□ 字体大小匹配（标题、正文、小字）
□ 字重正确（粗体、正常）
□ 行高合适
```

#### 布局验证

```
□ Section宽度一致
□ 内边距（padding）匹配
□ 外边距（margin）匹配
□ 网格布局正确
□ 元素对齐正确
```

#### 响应式验证

```
□ 桌面端 (>992px)
  □ 布局正常
  □ 所有内容可见
  □ 导航正常

□ 平板端 (768px - 991px)
  □ 布局自适应
  □ 文字大小合适
  □ 图片缩放正确

□ 移动端 (<768px)
  □ 单列布局
  □ 导航收起（如有汉堡菜单）
  □ 文字可读
  □ 按钮可点击
```

---

### 4. 数据完整性验证

**验证数据数量是否与原网站一致**

```python
def verify_data_completeness(original_site_data, react_data_files):
    """验证数据完整性"""
    
    results = {}
    
    # 对比导航项数量
    original_nav_count = count_nav_items_from_html('original_site/index.html')
    react_nav_count = len(json.load(open('src/data/navigationData.ts')))
    results['navigation'] = {
        'original': original_nav_count,
        'react': react_nav_count,
        'match': original_nav_count == react_nav_count
    }
    
    # 对比卡片数量
    original_cards_count = count_cards_from_html('original_site/index.html')
    react_cards_count = len(json.load(open('src/data/cardsData.ts')))
    results['cards'] = {
        'original': original_cards_count,
        'react': react_cards_count,
        'match': original_cards_count == react_cards_count
    }
    
    # 其他数据类型...
    
    return results
```

**验证清单**：

```
数据类型            | 原网站 | React项目 | 匹配
-------------------|--------|-----------|------
导航项              |   7    |    7      |  ✓
服务卡片            |  12    |   12      |  ✓
特性列表            |   6    |    6      |  ✓
评价/推荐           |   4    |    4      |  ✓
FAQ项              |   8    |    8      |  ✓
Footer链接组        |   4    |    4      |  ✓
```

---

### 5. 代码质量验证

#### TypeScript检查

```bash
# 运行类型检查
npx tsc --noEmit

# 预期：0 errors
```

#### Lint检查（如果配置了）

```bash
# ESLint检查
npm run lint

# 预期：0 errors, 0 warnings（或可接受的warning）
```

#### Console检查

```
打开浏览器Console (F12)
检查是否有：
□ 无 errors
□ 无 warnings（或只有可接受的warnings）
□ 无 404错误（资源加载失败）
```

---

## 📤 验证报告

### 生成验证报告

```json
{
  "verification_report": {
    "timestamp": "2026-01-26T15:30:00",
    "version": "3.1",
    
    "build_status": {
      "npm_install": "✓ success",
      "typescript_errors": 0,
      "build_success": true,
      "dev_server_running": true
    },
    
    "content_completeness": {
      "total_sections": 15,
      "sections_with_content": 15,
      "placeholders_found": 0,
      "empty_sections": 0,
      "score": "100%"
    },
    
    "visual_similarity": {
      "colors_match": true,
      "fonts_match": true,
      "layout_match": true,
      "responsive_working": true,
      "estimated_similarity": "97%"
    },
    
    "data_completeness": {
      "navigation_items": {
        "original": 7,
        "implemented": 7,
        "match": true
      },
      "cards": {
        "original": 12,
        "implemented": 12,
        "match": true
      },
      "faq_items": {
        "original": 8,
        "implemented": 8,
        "match": true
      },
      "total_score": "100%"
    },
    
    "code_quality": {
      "typescript_errors": 0,
      "eslint_errors": 0,
      "console_errors": 0,
      "all_components_rendered": true
    },
    
    "overall_status": "✓ PASSED",
    "overall_score": "98%",
    
    "issues_found": [],
    
    "recommendations": [
      "考虑添加loading状态",
      "可以优化图片加载（lazy loading已实现）",
      "建议添加错误边界处理"
    ]
  }
}
```

---

## ✅ 最终检查点

### 必须满足的条件

- [ ] **构建成功**
  - [ ] npm install 无错误
  - [ ] npm run build 成功
  - [ ] TypeScript 0错误
  - [ ] npm run dev 成功启动

- [ ] **内容完整性 100%** ⭐⭐⭐
  - [ ] 所有section都存在
  - [ ] 无占位符
  - [ ] 无 "Content goes here"
  - [ ] 无 TODO注释
  - [ ] 所有文字内容正确
  - [ ] 所有图片显示正确
  - [ ] 所有链接正确

- [ ] **视觉还原度 ≥ 95%**
  - [ ] 颜色完全匹配
  - [ ] 字体大小匹配
  - [ ] 布局结构匹配
  - [ ] 间距误差 < 5%
  - [ ] 响应式正常

- [ ] **数据完整性 100%**
  - [ ] 导航项数量 = 原网站
  - [ ] 卡片数量 = 原网站
  - [ ] 列表项数量 = 原网站
  - [ ] 不是样例数据

- [ ] **代码质量**
  - [ ] TypeScript 0错误
  - [ ] 所有组件可渲染
  - [ ] Console无error

---

## 🚨 不通过的情况

### 如果发现占位符

```
❌ 失败原因：发现占位符
位置：Section 5
内容："Content goes here"

解决方案：
1. 找到 Section5.tsx
2. 读取 section_5_content.json
3. 用真实内容替换占位符
4. 重新验证
```

### 如果数据不完整

```
❌ 失败原因：数据不完整
原网站卡片数：12
React项目卡片数：2

解决方案：
1. 重新检查原HTML
2. 提取所有12个卡片的数据
3. 更新 cardsData.ts
4. 重新验证
```

### 如果section缺失

```
❌ 失败原因：section缺失
原网站section数：15
React项目section数：8

解决方案：
1. 检查 sections_structure.json
2. 找到缺失的section (9-15)
3. 为每个缺失的section生成组件
4. 在App.tsx中添加
5. 重新验证
```

---

## 📊 质量评分标准

### A级（95-100分）

```
✓ 所有验证项都通过
✓ 视觉还原度 ≥ 95%
✓ 内容完整性 100%
✓ 数据完整性 100%
✓ 无任何占位符
✓ 代码质量优秀
```

### B级（85-94分）

```
✓ 大部分验证项通过
✓ 视觉还原度 85-94%
✓ 内容完整性 ≥ 95%
✓ 可能有1-2个小的样式差异
✓ 代码质量良好
```

### C级（75-84分）

```
- 部分验证项通过
- 视觉还原度 75-84%
- 内容基本完整
- 有一些样式差异
- 代码可运行但有改进空间
```

### D级（<75分）- 不合格

```
✗ 多个验证项失败
✗ 视觉还原度 < 75%
✗ 内容不完整或有占位符
✗ 数据缺失
✗ 需要重做
```

---

## 💡 验证技巧

### 1. 使用浏览器DevTools

```javascript
// Console中运行，快速检查占位符
const placeholders = [
  'content goes here',
  'todo',
  'placeholder',
  'lorem ipsum'
];

const bodyText = document.body.innerText.toLowerCase();
const found = placeholders.filter(p => bodyText.includes(p));

console.log(found.length === 0 ? '✓ 无占位符' : `✗ 发现占位符: ${found}`);
```

### 2. 截图对比

```python
# 使用截图对比工具
from selenium import webdriver
from PIL import Image
import imagehash

def compare_screenshots(url1, url2):
    """截图并对比两个网站"""
    # 截取两个网站的截图
    # 计算图片hash
    # 对比相似度
    pass
```

### 3. 元素数量对比

```javascript
// 对比section数量
console.log('Section数量:', document.querySelectorAll('section').length);

// 对比导航项数量
console.log('导航项数量:', document.querySelectorAll('nav a').length);

// 对比卡片数量
console.log('卡片数量:', document.querySelectorAll('.card').length);
```

---

[← 上一步：模块07 - 代码生成](./07_代码生成.md) | [→ 附录：常见错误及解决方案](./常见错误及解决方案.md)
