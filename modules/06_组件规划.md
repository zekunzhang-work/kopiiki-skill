# 模块 06：组件规划

[← 返回主文档](../AI_AGENT_视觉还原_SOP_3.1_主文档.md)

---

## 🎯 目标

规划React组件架构，区分**通用组件**和**内容组件**。

---

## 📋 组件分类

### 1. 布局组件（Layout Components）

**特征**：可复用的页面框架

```
src/components/layout/
├── Navigation.tsx       // 导航栏
├── Header.tsx          // 页头（如果独立于导航）
├── Footer.tsx          // 页脚
└── Modal.tsx           // 弹窗（如果有）
```

---

### 2. UI组件（UI Components）

**特征**：可复用的UI元素

```
src/components/ui/
├── Card.tsx            // 卡片组件
├── Button.tsx          // 按钮组件
├── Cta.tsx             // CTA按钮组件
├── Form.tsx            // 表单组件
├── Input.tsx           // 输入框组件
├── Section.tsx         // Section容器组件
└── Image.tsx           // 图片组件
```

---

### 3. Section组件（Section Components）

**特征**：包含具体内容的section

```
src/components/sections/
├── HeroSection.tsx          // Section 1 - Hero
├── ServicesSection.tsx      // Section 2 - 服务
├── FeaturesSection.tsx      // Section 3 - 特性
├── RulesSection.tsx         // Section 4 - 规则
├── TestimonialsSection.tsx  // Section 5 - 评价
├── PricingSection.tsx       // Section 6 - 价格
├── FAQSection.tsx           // Section 7 - FAQ
├── CTASection.tsx           // Section 8 - CTA
└── ... (根据实际section数量)
```

**重要**：Section组件数量 = 实际section数量（不是10个通用组件）

---

## 📋 执行步骤

### 步骤1：统计组件需求

**基于已有信息统计**

```python
import json

def plan_components(sections_structure):
    """规划组件架构"""
    with open(sections_structure, 'r') as f:
        data = json.load(f)
    
    plan = {
        'layout': [],
        'ui': [],
        'sections': []
    }
    
    # 布局组件（固定）
    plan['layout'] = [
        'Navigation',
        'Header',
        'Footer',
        'Modal'
    ]
    
    # UI组件（根据检测到的元素）
    ui_components = set()
    
    for section in data['sections_map']:
        # 如果有表单
        if section.get('has_forms'):
            ui_components.add('Form')
            ui_components.add('Input')
        
        # 如果是卡片布局
        if 'grid' in section.get('layout_type', ''):
            ui_components.add('Card')
        
        # 通用组件
        ui_components.add('Button')
        ui_components.add('Section')
        ui_components.add('Image')
    
    plan['ui'] = sorted(list(ui_components))
    
    # Section组件（每个section一个）
    for section in data['sections_map']:
        component_name = generate_section_component_name(section)
        plan['sections'].append({
            'name': component_name,
            'index': section['index'],
            'type': section['type'],
            'file': f"{component_name}.tsx"
        })
    
    return plan

def generate_section_component_name(section):
    """生成section组件名称"""
    # 优先使用语义化名称
    type_mapping = {
        'hero': 'HeroSection',
        'services': 'ServicesSection',
        'features': 'FeaturesSection',
        'testimonials': 'TestimonialsSection',
        'pricing': 'PricingSection',
        'faq': 'FAQSection',
        'cta': 'CTASection',
        'team': 'TeamSection',
        'blog': 'BlogSection',
        'contact': 'ContactSection',
        'form': 'FormSection'
    }
    
    section_type = section['type'].lower()
    
    if section_type in type_mapping:
        return type_mapping[section_type]
    else:
        # 使用Section + 索引
        return f"Section{section['index']}"

# 执行
component_plan = plan_components('sections_structure.json')

# 保存
with open('component_plan.json', 'w', encoding='utf-8') as f:
    json.dump(component_plan, f, indent=2, ensure_ascii=False)
```

---

### 步骤2：生成组件清单

**输出详细的组件清单**

```python
def generate_component_checklist(component_plan):
    """生成组件自检清单"""
    
    checklist = "# 组件自检清单\n\n"
    
    checklist += "## 布局组件 (Layout Components)\n\n"
    for comp in component_plan['layout']:
        checklist += f"- [ ] `{comp}.tsx`\n"
    
    checklist += f"\n**总计**: {len(component_plan['layout'])} 个\n\n"
    
    checklist += "---\n\n"
    checklist += "## UI组件 (UI Components)\n\n"
    for comp in component_plan['ui']:
        checklist += f"- [ ] `{comp}.tsx`\n"
    
    checklist += f"\n**总计**: {len(component_plan['ui'])} 个\n\n"
    
    checklist += "---\n\n"
    checklist += "## Section组件 (Section Components)\n\n"
    checklist += "⚠️ **每个section必须有对应的组件，不能遗漏！**\n\n"
    
    for section in component_plan['sections']:
        checklist += f"- [ ] `{section['file']}` (Section {section['index']} - {section['type']})\n"
    
    checklist += f"\n**总计**: {len(component_plan['sections'])} 个\n\n"
    
    checklist += "---\n\n"
    checklist += f"## 组件总数\n\n"
    total = len(component_plan['layout']) + len(component_plan['ui']) + len(component_plan['sections'])
    checklist += f"- 布局组件: {len(component_plan['layout'])}\n"
    checklist += f"- UI组件: {len(component_plan['ui'])}\n"
    checklist += f"- Section组件: {len(component_plan['sections'])}\n"
    checklist += f"- **总计**: {total}\n"
    
    return checklist

# 生成并保存
checklist = generate_component_checklist(component_plan)
with open('components_check.md', 'w', encoding='utf-8') as f:
    f.write(checklist)

print("✓ 组件清单已生成: components_check.md")
```

---

### 步骤3：规划组件依赖关系

**明确哪些组件会使用哪些子组件**

```python
def plan_component_dependencies(component_plan, sections_content):
    """规划组件依赖关系"""
    
    dependencies = {}
    
    # Section组件的依赖
    for section_comp in component_plan['sections']:
        section_idx = section_comp['index']
        
        # 查找该section的内容
        section_data = next(
            (s for s in sections_content if s['section_index'] == section_idx),
            None
        )
        
        if not section_data:
            continue
        
        deps = set()
        
        # 检查使用的UI组件
        structure = section_data['structure']
        
        if structure.get('images'):
            deps.add('Image')
        
        if structure.get('buttons'):
            deps.add('Button')
        
        if structure.get('forms'):
            deps.add('Form')
            deps.add('Input')
        
        # 如果有重复的卡片结构
        if len(structure.get('cards', [])) > 1:
            deps.add('Card')
        
        dependencies[section_comp['name']] = sorted(list(deps))
    
    return dependencies
```

---

## 📤 输出格式

### component_plan.json

```json
{
  "layout": [
    "Navigation",
    "Header",
    "Footer",
    "Modal"
  ],
  "ui": [
    "Button",
    "Card",
    "Form",
    "Image",
    "Input",
    "Section"
  ],
  "sections": [
    {
      "name": "HeroSection",
      "index": 1,
      "type": "hero",
      "file": "HeroSection.tsx"
    },
    {
      "name": "ServicesSection",
      "index": 2,
      "type": "services",
      "file": "ServicesSection.tsx"
    },
    {
      "name": "FeaturesSection",
      "index": 3,
      "type": "features",
      "file": "FeaturesSection.tsx"
    }
  ],
  "total_components": 23
}
```

---

### components_check.md

```markdown
# 组件自检清单

## 布局组件 (Layout Components)

- [ ] `Navigation.tsx`
- [ ] `Header.tsx`
- [ ] `Footer.tsx`
- [ ] `Modal.tsx`

**总计**: 4 个

---

## UI组件 (UI Components)

- [ ] `Button.tsx`
- [ ] `Card.tsx`
- [ ] `Form.tsx`
- [ ] `Image.tsx`
- [ ] `Input.tsx`
- [ ] `Section.tsx`

**总计**: 6 个

---

## Section组件 (Section Components)

⚠️ **每个section必须有对应的组件，不能遗漏！**

- [ ] `HeroSection.tsx` (Section 1 - hero)
- [ ] `ServicesSection.tsx` (Section 2 - services)
- [ ] `FeaturesSection.tsx` (Section 3 - features)
- [ ] `RulesSection.tsx` (Section 4 - content)
- [ ] `TestimonialsSection.tsx` (Section 5 - testimonials)
- [ ] `PricingSection.tsx` (Section 6 - pricing)
- [ ] `FAQSection.tsx` (Section 7 - faq)
- [ ] `CTASection.tsx` (Section 8 - cta)
- [ ] `Section9.tsx` (Section 9 - content)
- [ ] `Section10.tsx` (Section 10 - content)
- [ ] `Section11.tsx` (Section 11 - content)
- [ ] `Section12.tsx` (Section 12 - content)
- [ ] `Section13.tsx` (Section 13 - content)

**总计**: 13 个

---

## 组件总数

- 布局组件: 4
- UI组件: 6
- Section组件: 13
- **总计**: 23
```

---

## 📋 组件架构示例

### 容器 vs 内容组件

#### ❌ 错误理解：

```
只创建10个通用组件（对应components文件夹）
然后在App.tsx中用占位符
```

#### ✅ 正确理解：

```
通用组件（10个）+ Section内容组件（13个）= 23个组件

App.tsx组装所有section：
  <Navigation />
  <HeroSection />          ← 包含Hero的完整内容
  <ServicesSection />      ← 包含Section 2的完整内容
  <FeaturesSection />      ← 包含Section 3的完整内容
  ...
  <Footer />
```

---

### Section组件结构示例

**HeroSection.tsx**（内容组件）

```typescript
import { heroData } from '../data/heroData';
import Button from '../components/ui/Button';
import Image from '../components/ui/Image';

const HeroSection = () => {
  return (
    <section className="hero-section">
      <div className="w-layout-blockcontainer site-container w-container">
        <h1 className="text-60">{heroData.title}</h1>
        <p className="text-20">{heroData.description}</p>
        <Button href={heroData.cta.href} className="btn-primary">
          {heroData.cta.text}
        </Button>
        <Image src={heroData.image.src} alt={heroData.image.alt} />
      </div>
    </section>
  );
};

export default HeroSection;
```

**ServicesSection.tsx**（内容组件，使用Card子组件）

```typescript
import { servicesData } from '../data/servicesData';
import Card from '../components/ui/Card';

const ServicesSection = () => {
  return (
    <section className="py-100">
      <div className="w-layout-blockcontainer site-container w-container">
        <h2 className="text-30">{servicesData.title}</h2>
        <p className="text-18">{servicesData.description}</p>
        
        <div className="services-grid">
          {servicesData.cards.map((card) => (
            <Card key={card.id} {...card} />
          ))}
        </div>
      </div>
    </section>
  );
};

export default ServicesSection;
```

---

## ✅ 检查点

完成本阶段后，确认：

- [ ] 组件架构已规划
  - [ ] 布局组件清单 ✓
  - [ ] UI组件清单 ✓
  - [ ] Section组件清单 ✓

- [ ] 组件数量正确
  - [ ] 通用组件数 = components文件夹数
  - [ ] Section组件数 = 实际section数
  - [ ] **不能混淆两者**

- [ ] 组件依赖关系已明确
  - [ ] 知道哪些section使用哪些UI组件

- [ ] 输出文件已生成
  - [ ] component_plan.json ✓
  - [ ] components_check.md ✓

---

## 🚨 常见误解

### ❌ 误解1：只需要10个组件

**错误想法**：
```
原网站有10个components文件夹
所以只需要创建10个React组件
```

**正确理解**：
```
10个components文件夹 = 10个通用组件（Card, Button等）
15个section = 15个内容组件（HeroSection, ServicesSection等）
总共需要 25+ 个组件
```

---

### ❌ 误解2：Section组件应该是空容器

**错误做法**：
```tsx
// Section.tsx - 通用容器
const Section = ({ children, className }) => {
  return <section className={className}>{children}</section>;
};

// App.tsx
<Section className="py-100">
  {/* 用户需要自己填充内容？❌ */}
</Section>
```

**正确做法**：
```tsx
// ServicesSection.tsx - 包含完整内容的组件
const ServicesSection = () => {
  return (
    <section className="py-100">
      <div className="container">
        <h2>我们的服务</h2>
        <p>专业的服务...</p>
        {servicesData.map(s => <Card key={s.id} {...s} />)}
      </div>
    </section>
  );
};
```

---

## 💡 组件命名建议

### 语义化命名优先

```
hero section → HeroSection.tsx
services section → ServicesSection.tsx
testimonials section → TestimonialsSection.tsx
```

### 无语义时使用索引

```
第4个section（无明确语义） → Section4.tsx
第9个section（通用内容） → Section9.tsx
```

### 保持一致性

```
✅ 好：HeroSection, ServicesSection, Section4
❌ 差：Hero, ServicesSection, ContentSection4（不一致）
```

---

[← 上一步：模块05 - 数据分离](./05_数据分离.md) | [→ 下一步：模块07 - 代码生成](./07_代码生成.md)
